# Local-LLM-Bot
A lightweight Stream-lit web application that interfaces with local LLMs via Ollama's API for private, offline conversations. Features a persistent chat history session state and instant, non-streamed responses from models like Phi-3.
